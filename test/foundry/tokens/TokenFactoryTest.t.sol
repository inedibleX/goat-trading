// SPDX-License-Identifier: UNLICENSED
pragma solidity 0.8.19;

import "./BaseTokenTest.t.sol";

contract TokenFactoryTest is BaseTokenTest {
    // Test that each of the tokens that were setup were created successfully by the factory.
    function testCreateTokenSuccessPlainToken() public {
        // Check name, symbol, total supply, who owns tokens, etc.
    }
    function testCreateTokenSuccessDemurrageToken() public {
        // Check name, symbol, total supply, who owns tokens, etc.
    }
    function testcreatetokensuccessTaxtoken() public {
        // Check name, symbol, total supply, who owns tokens, etc.
    }
    function testcreatetokensuccessTaxShareToken() public {
        // Check name, symbol, total supply, who owns tokens, etc.
    }
    function testcreatetokensuccessTaxBurnToken() public {
        // Check name, symbol, total supply, who owns tokens, etc.
    }
    function testcreatetokensuccessDividendToken() public {
        // Check name, symbol, total supply, who owns tokens, etc.
    }
    function testcreatetokensuccessValutToken() public {
        // Check name, symbol, total supply, who owns tokens, etc.
    }
}
